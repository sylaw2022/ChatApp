{
  "name": "chat-app-sse",
  "version": "1.0.0",
  "scripts": {
    "start": "node api/index.js",
    "dev": "node api/index.js",
    "client": "cd client && npm run dev",
    "build": "cd client && npm ci --prefer-offline --no-audit && npm run build",
    "vercel-build": "cd client && npm install --prefer-offline --no-audit && npm run build"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cloudinary": "^1.41.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.0.3",
    "multer": "^2.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0" 
  }
}
